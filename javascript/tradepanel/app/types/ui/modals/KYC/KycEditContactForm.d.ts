import Modal from "../Modal";
export default class KycEditContactForm {
    modal: Modal;
    constructor();
    show(): Promise<void>;
    private bindEvents;
    private showError;
    private validate;
}
