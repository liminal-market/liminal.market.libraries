import Modal from "../Modal";
export default class Registration {
    modal: Modal;
    constructor();
    show(): void;
    private bindEvents;
    hideError(): void;
    showError(message: string): void;
}
