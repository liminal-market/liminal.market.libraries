export default class FormHelper {
    static getParams(selector: string): any;
    private static serialize;
    static fillInputs(properties: string[], item: any): void;
    static validate(selector: string): boolean;
}
